# -----------------------------------------------------------------------------
# DPDP PII REDACTION PLATFORM - ENVIRONMENT CONFIGURATION
# -----------------------------------------------------------------------------

# --- Backend API Settings ---
# Generate a secure key using: openssl rand -hex 32
SECRET_KEY=70921cf8a0e86a079e0a6d0c6d7a421b
PROJECT_NAME="DPDP PII Redaction Platform"
API_V1_STR="/api/v1"

# --- Database Configuration ---
# SQLALCHEMY_DATABASE_URI will be constructed automatically from these if not provided
POSTGRES_SERVER=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pii_platform
POSTGRES_PORT=5432

# --- Redis & Celery Settings ---
REDIS_HOST=localhost
REDIS_PORT=6379
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# --- Frontend Configuration ---
# The URL where the backend is accessible to the frontend
VITE_API_BASE_URL=http://localhost:8002
